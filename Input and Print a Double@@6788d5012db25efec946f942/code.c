#include <stdio.h>

int main() {
    double num;

    // Input: Reading a double value
    scanf("%lf", &num);

    // Output: Printing the entered double
    printf("You entered: %.4lf\n", num);

    return 0;
}
