#include <stdio.h>

int main() {
    int a, b;

    // Taking input
    scanf("%d %d", &a, &b);

    // Checking condition and printing result
    if (a > b) {
        printf("True\n");
    } else {
        printf("False\n");
    }

    return 0;
}